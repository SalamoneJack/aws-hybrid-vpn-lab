# strongSwan PSK file — place at: /etc/ipsec.secrets
# Replace <CLOUD_EIP> and <ONPREM_EIP> with values from terraform output
# Use the same PSK on both instances
# chmod 600 /etc/ipsec.secrets

<CLOUD_EIP> <ONPREM_EIP> : PSK "replace-with-a-strong-random-passphrase"
